var speed = 5;
$(function() {
	$(".a1").typed({
		strings: ["private"],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".a2").typed({
				strings: [" void "],
				typeSpeed: speed,
				cursorChar: "|",
				callback: function() {
					$(".typed-cursor").fadeOut("fast");
					$(".a3").typed({
						strings: [" generateWebsite"],
						typeSpeed: speed,
						cursorChar: "|",
						callback: function() {
							$(".typed-cursor").fadeOut("fast");
							$(".a4").typed({
								strings: ["()"],
								typeSpeed: speed,
								cursorChar: "|",
								callback: function() {
									$(".typed-cursor").fadeOut("fast");
									$(".a5").typed({
										strings: ["{"],
										typeSpeed: speed,
										cursorChar: "|",
										callback: function() {
											$(".typed-cursor").fadeOut("fast");							
											$(".s1").typed({
												strings: ["&nbsp;&nbsp;&nbsp;&nbsp;string"],
												typeSpeed: speed,
												cursorChar: "|",
												callback: function() {
													$(".typed-cursor").fadeOut("fast");
													$(".s2").typed({
														strings: [" name "],
														typeSpeed: speed,
														cursorChar: "|",
														callback: function() {
															$(".typed-cursor").fadeOut("fast");
															$(".s3").typed({
																strings: [" = "],
																typeSpeed: speed,
																cursorChar: "|",
																callback: function() {
																	$(".typed-cursor").fadeOut("fast");
																	$(".s4").typed({
																		strings: ["\"Tristan Fitzwater\""],
																		typeSpeed: 20,
																		cursorChar: "|",
																		callback: function() {
																			$(".typed-cursor").fadeOut("fast");
																			$(".s5").typed({
																				strings: [";"],
																				typeSpeed: speed,
																				cursorChar: "|",
																				callback: function() {
																							$(".b1").typed({
		strings: ["&nbsp;&nbsp;&nbsp;&nbsp;String"],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".b2").typed({
		strings: ["[]"],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".b3").typed({
		strings: [" bio"],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".b4").typed({
		strings: [" ="],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".b5").typed({
		strings: [" new"],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".b6").typed({
		strings: [" String"],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".b7").typed({
		strings: ["["],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".b8").typed({
		strings: ["3"],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
			$(".b9").typed({
		strings: ["];"],
		typeSpeed: speed,
		cursorChar: "|",
		callback: function() {
			$(".typed-cursor").fadeOut("fast");
		}
				})
																		}
																				})
																		}
																		})
																		}
																})
																		}
														})
																		}
												})
																		}
		})
																		}
		})
																		}
		})
																		}
																			})
																		}
																	})
																}
															})
														}
													})
												}
											})
										}
									})
								}
							})
						}
					})
				}
			})
		}
	})
});
